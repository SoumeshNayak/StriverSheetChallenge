def NthRoot(n: int, m: int) -> int:
    # Write Your Code Here
    pass
    l,r=1,m
    while l<=r:
        mid=(l+r)//2
        if mid**n==m:
            return mid
        elif mid**n>m:
            r=mid-1
        else:
            l=mid+1
    return -1                
